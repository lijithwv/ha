# Loads default set of integrations. Do not remove.
default_config:

template: !include templates.yaml
rest_command: !include rest_commands.yaml
sensor: !include sensors.yaml
shell_command: !include shell_commands.yaml
scene: !include scenes.yaml

light:
  - platform: group
    name: Interior Lights
    entities:
      - light.living_corner_lamp
      - light.living_table_lamp
      - light.anko_wi_fi_sunset_light
  - platform: group
    name: Exterior Lights
    entities:
      - light.porch_light
      - light.backyard_floodlight

# Scripts
script:
  - !include scripts.yaml
  - !include_dir_merge_named scripts/

# Automations
automation ui: !include automations.yaml
automation manual: !include_dir_merge_list automations/

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

recorder:
  purge_keep_days: 3
  auto_purge: true
  exclude:
    entity_globs:
      - sensor.home_extended_text_*

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 172.30.33.0/24

ffmpeg:

homeassistant:
  allowlist_external_dirs:
    - "/config"
    - "/config/www"

# Notification group
notify:
  - platform: group
    name: "ALL_DEVICES"
    services:
      - action: mobile_app_iphone_l
      - action: mobile_app_sreelakshmis_iphone
