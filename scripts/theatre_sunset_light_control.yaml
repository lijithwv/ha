theatre_sunset_light_control:
  alias: "Theatre Sunset Light Control"
  description: "Turn the theatre sunset light on or off, with optional brightness"
  fields:
    state:
      description: "Desired state: 'on' or 'off'"
      example: "on"
    brightness:
      description: "Brightness percentage when turning on (1-100)"
      example: 100
      required: false
  sequence:
    - if:
        - condition: template
          value_template: "{{ states('light.anko_wi_fi_sunset_light') != state }}"
      then:
        - choose:
            - conditions:
                - condition: template
                  value_template: "{{ state == 'on' }}"
              sequence:
                - service: light.turn_on
                  target:
                    entity_id: light.anko_wi_fi_sunset_light
                  data:
                    brightness_pct: "{{ brightness | default(100) }}"
            - conditions:
                - condition: template
                  value_template: "{{ state == 'off' }}"
              sequence:
                - service: light.turn_off
                  target:
                    entity_id: light.anko_wi_fi_sunset_light